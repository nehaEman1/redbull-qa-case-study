| ID | Scenario | Expected Result | Actual Result | Result | Issue
|---|---|---|---|---|---|
| API-01 | Send a valid `update_core_services` command to an online device | Request returns `200`; response identifies updated device; re-fetching the device shows updated `core_services_status` | POST returned `200 OK` and response identified device `79101X02Q006001000BFJ2002` as updated. Re-fetch via `GET /api/devices/295396` returned `core_services_status = up-to-date`. Device was already `up-to-date` before execution, so an observable status transition could not be verified with the available test data. | PASS WITH TEST DATA LIMITATION |
| API-02 | Send a valid `update_core_services` command to an offline device | Request is rejected with a clear, appropriate error response indicating the device is offline | Request using offline device `0JM1M6210063` returned `500 Internal Server Error`. Response stated that the device was offline but also exposed an internal application stack trace. | FAIL | #17
| API-03 | Send request with unsupported `command_name` | Request is rejected with a 4xx response and clear validation error | Returned `400 Bad Request` with `"error": "Unsupported command_name"` | PASS |
| API-04 | Send command using a non-existent `device_id` | Request should be rejected with a clear 4xx validation/not-found error | Returned `200 OK` with `"updated": []` | FAIL | #18
| API-05 | Send command with whitespace-only `command_version` | Request should be rejected with a clear 4xx validation error | Returned `200 OK` and reported the device as updated | FAIL | #19
| API-06 | Send command without the `devices` field | Request should be rejected with a clear 4xx validation error | Returned `400 Bad Request` with `"devices must be a non-empty array of device_id strings"` | PASS |
| API-08 | Send valid command without Authorization | Request should be rejected | Returned `401 Unauthorized ` | PASS |
| API-09 | Send valid command with invalid Bearer token | Request should be rejected | Returned `401 Unauthorized ` | PASS |
| API-10 | Send command with an empty `devices` array | Request should be rejected with a clear 4xx validation error | Returned `400 Bad Request ` "devices must be a non-empty array of device_id strings"` | PASS |
| API-11 | Send command with one online and one offline device | Behavior for mixed batches is not defined; API should return a clear, non-5xx response explaining the outcome and should not expose internal details | Returned `500 Internal Server Error`; response identified the offline device but exposed an internal stack trace. Batch success/partial-update behavior is unclear from the response. | FAIL |#17
| API-12 | Send `update_core_services` with malformed/unexpected `command_version` | Invalid version value should be rejected or clearly validated | Returned `200 OK` and reported device as updated, while actual service versions remained normal release values | FAIL | #19
